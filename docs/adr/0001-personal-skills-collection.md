# Personal collection, not a two-skill AppSec package

The published GitHub repo is `daviduartedev/skills`. V0.1 shipped as exactly two SDD AppSec skills. The collection is now those two plus `smoke-test-list` (a human smoke gate after implement). The README follows a personal-collection shape (install, why, categorised reference) without copying another author's inventory. Further skills still need a spec; scanners and SDD orchestrators stay out.
